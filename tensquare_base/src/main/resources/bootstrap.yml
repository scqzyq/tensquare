spring:
  cloud:
    config:
      name: base # 不使用profile和label配置 使用默认环境
#      discovery:
#        enabled: true
#        service-id: tensquare-config
      uri: http://192.168.167.130:12000

logging:
  level:
    com.netflix.discovery: 'OFF'
    org.springframework.cloud: 'DEBUG'
